# Lifecycle artifacts

`lifecycle_summary.json`, `quality.json`, `transition_matrix.json`, `monthly_timing.json`, and `relationships.json` are generated serving artifacts. `monthly_timing.json` is a compact research-friendly lifecycle summary at request grain. Lifecycle snapshots are provenance-bearing state deltas with schema version 1.
